Given the user input, the system first parses the function to identify its purpose, parameters, and return value. 
Then, it generates a commented version of the function based on this analysis.
Return only commented code.